Tests expect the repository root at `tests/governance/../..` (two parents up) and `tests/skills/spec-dev/../..` (three parents up), so they must be run from within this workspace. The spec-dev tests require the `.codex/skills/spec-dev` directory tree (schemas, evals/cases, evals/runs) to exist on disk, and the governance tests require `scripts/check-agent-governance.sh` and `.codex/agents/*.toml` files to be present.