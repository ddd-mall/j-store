Two independent Gradle sub-projects sharing the same group `com.jstore` and Kotlin JVM toolchain (JDK 25). Each module follows a standard Gradle layout with `src/main/kotlin`, `src/test/kotlin`, and `src/main/resources`. Both currently contain only a generated `Main.kt` entry point under package `com.jstore` with no business code yet, indicating this is an empty scaffold awaiting domain implementation. There is no declared dependency between the two modules in their build files, so any cross-module linkage would need to be added explicitly.