Run with `./gradlew bootRun` (defaults to `local` profile). Database schema is bootstrapped via Flyway baseline version `20260507` against PostgreSQL; local dev connects to `192.168.31.213:30432/j_store` schema `develop`. Tests require no external DB thanks to `io.zonky.test:embedded-postgres:2.1.0`. Docker image is produced by the included `Dockerfile` using `amazoncorretto:21.0.8-al2023-headless`.