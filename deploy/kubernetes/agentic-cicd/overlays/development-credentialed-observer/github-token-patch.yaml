apiVersion: apps/v1
kind: Deployment
metadata:
  name: symphony
  namespace: agentic-cicd
spec:
  template:
    spec:
      containers:
        - name: symphony
          env:
            - name: JSTORE_SYMPHONY_GITHUB_TOKEN
              value: null
              valueFrom:
                secretKeyRef:
                  name: symphony-github-token
                  key: token
                  optional: false
