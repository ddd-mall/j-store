diff --git a/elixir/lib/symphony_elixir/agent_runner.ex b/elixir/lib/symphony_elixir/agent_runner.ex
index 9efbac7..15edea4 100644
--- a/elixir/lib/symphony_elixir/agent_runner.ex
+++ b/elixir/lib/symphony_elixir/agent_runner.ex
@@ -5,7 +5,7 @@ defmodule SymphonyElixir.AgentRunner do

   require Logger
   alias SymphonyElixir.Codex.AppServer
-  alias SymphonyElixir.{Config, PromptBuilder, Tracker, Workspace}
+  alias SymphonyElixir.{AgenticCicd, Config, PromptBuilder, Tracker, Workspace}
   alias SymphonyElixir.Tracker.Issue

   @type worker_host :: String.t() | nil
@@ -54,12 +54,21 @@ defmodule SymphonyElixir.AgentRunner do
           end

         after_result =
-          Workspace.run_after_run_hook(workspace, issue, worker_host, turn_receipt)
+          case run_result do
+            {:skip_model, _phase_context} ->
+              :ok
+
+            _ ->
+              Workspace.run_after_run_hook(workspace, issue, worker_host, turn_receipt)
+          end

         case {run_result, after_result} do
           {{:ok, _receipt}, :ok} ->
             :ok

+          {{:skip_model, _phase_context}, :ok} ->
+            :ok
+
           {{:ok, _receipt}, {:error, reason}} ->
             {:error, reason}

@@ -103,12 +112,52 @@ defmodule SymphonyElixir.AgentRunner do
   defp send_worker_runtime_info(_recipient, _issue, _worker_host, _workspace), do: :ok

   defp run_codex_turns(workspace, issue, codex_update_recipient, opts, worker_host) do
+    with {:ok, phase_context} <- AgenticCicd.phase_context(workspace, issue, worker_host) do
+      case phase_context do
+        %{"run_model" => true} ->
+          run_codex_model(
+            workspace,
+            issue,
+            codex_update_recipient,
+            opts,
+            worker_host,
+            phase_context
+          )
+
+        %{"run_model" => false} ->
+          {:skip_model, phase_context}
+      end
+    end
+  end
+
+  defp run_codex_model(workspace, issue, codex_update_recipient, opts, worker_host, phase_context) do
+    model_workspace = phase_context["model_workspace"]
     max_turns = Keyword.get(opts, :max_turns, Config.settings!().agent.max_turns)
     issue_state_fetcher = Keyword.get(opts, :issue_state_fetcher, &Tracker.fetch_issues_by_ids/1)
+    opts = Keyword.put(opts, :agentic_cicd_context, phase_context)

-    with {:ok, session} <- AppServer.start_session(workspace, worker_host: worker_host) do
+    with {:ok, session} <-
+           AppServer.start_session(model_workspace,
+             worker_host: worker_host,
+             runtime_policy: phase_context
+           ) do
       try do
-        do_run_codex_turns(session, workspace, issue, codex_update_recipient, opts, issue_state_fetcher, 1, max_turns)
+        case do_run_codex_turns(
+               session,
+               workspace,
+               issue,
+               codex_update_recipient,
+               opts,
+               issue_state_fetcher,
+               1,
+               max_turns
+             ) do
+          {:ok, receipt} ->
+            {:ok, Map.put(receipt, :agentic_cicd_context, phase_context)}
+
+          other ->
+            other
+        end
       after
         AppServer.stop_session(session)
       end
diff --git a/elixir/lib/symphony_elixir/agentic_cicd.ex b/elixir/lib/symphony_elixir/agentic_cicd.ex
new file mode 100644
index 0000000..3ea4702
--- /dev/null
+++ b/elixir/lib/symphony_elixir/agentic_cicd.ex
@@ -0,0 +1,91 @@
+defmodule SymphonyElixir.AgenticCicd do
+  @moduledoc """
+  Reads j-store phase policy from the image-owned trusted controller.
+  """
+
+  @controller "/opt/jstore-agentic-controller/controller.py"
+  @allowed_roles ["observer", "implementer", "reviewer", "none"]
+  @allowed_thread_sandboxes ["read-only", "workspace-write"]
+
+  @spec phase_context(Path.t(), map(), String.t() | nil) :: {:ok, map()} | {:error, term()}
+  def phase_context(_workspace, _issue, worker_host) when is_binary(worker_host) do
+    {:error, :agentic_cicd_remote_worker_not_supported}
+  end
+
+  def phase_context(workspace, %{identifier: identifier}, nil)
+      when is_binary(workspace) and is_binary(identifier) do
+    case System.cmd(
+           "/usr/bin/python3",
+           [
+             @controller,
+             "phase-context",
+             "--issue",
+             identifier,
+             "--workspace",
+             workspace
+           ],
+           stderr_to_stdout: true
+         ) do
+      {output, 0} -> decode_context(output)
+      {output, status} -> {:error, {:agentic_cicd_phase_context_failed, status, output}}
+    end
+  end
+
+  def phase_context(_workspace, _issue, _worker_host), do: {:error, :invalid_agentic_cicd_issue}
+
+  defp decode_context(output) do
+    with {:ok, context} when is_map(context) <- Jason.decode(String.trim(output)),
+         :ok <- validate_context(context) do
+      {:ok, context}
+    else
+      {:error, reason} -> {:error, {:invalid_agentic_cicd_phase_context, reason}}
+      _ -> {:error, :invalid_agentic_cicd_phase_context}
+    end
+  end
+
+  defp validate_context(context) do
+    required = [
+      "phase",
+      "role",
+      "run_model",
+      "complete_turn",
+      "thread_sandbox",
+      "turn_sandbox_policy",
+      "head_sha",
+      "candidate_revision",
+      "model_workspace"
+    ]
+
+    cond do
+      Enum.sort(Map.keys(context)) != Enum.sort(required) ->
+        {:error, :unexpected_fields}
+
+      context["role"] not in @allowed_roles ->
+        {:error, :invalid_role}
+
+      context["thread_sandbox"] not in @allowed_thread_sandboxes ->
+        {:error, :invalid_thread_sandbox}
+
+      not is_boolean(context["run_model"]) or not is_boolean(context["complete_turn"]) ->
+        {:error, :invalid_boolean}
+
+      not is_map(context["turn_sandbox_policy"]) ->
+        {:error, :invalid_turn_sandbox_policy}
+
+      not is_binary(context["head_sha"]) or
+          not String.match?(context["head_sha"], ~r/^[0-9a-f]{40}$/) ->
+        {:error, :invalid_head_sha}
+
+      context["candidate_revision"] != nil and
+          (not is_binary(context["candidate_revision"]) or
+             not String.match?(context["candidate_revision"], ~r/^[0-9a-f]{64}$/)) ->
+        {:error, :invalid_candidate_revision}
+
+      not is_binary(context["model_workspace"]) or context["model_workspace"] == "" ->
+        {:error, :invalid_model_workspace}
+
+      true ->
+        :ok
+    end
+  end
+end
diff --git a/elixir/lib/symphony_elixir/codex/app_server.ex b/elixir/lib/symphony_elixir/codex/app_server.ex
index ae3db62..fa79d58 100644
--- a/elixir/lib/symphony_elixir/codex/app_server.ex
+++ b/elixir/lib/symphony_elixir/codex/app_server.ex
@@ -43,8 +43,10 @@ defmodule SymphonyElixir.Codex.AppServer do
     with {:ok, expanded_workspace} <- validate_workspace_cwd(workspace, worker_host),
          {:ok, port} <- start_port(expanded_workspace, worker_host, dynamic_tool_binding) do
       metadata = port_metadata(port, worker_host)
+      runtime_policy = Keyword.get(opts, :runtime_policy)

-      with {:ok, session_policies} <- session_policies(expanded_workspace, worker_host),
+      with {:ok, base_policies} <- session_policies(expanded_workspace, worker_host),
+           {:ok, session_policies} <- apply_runtime_policy(base_policies, runtime_policy),
            {:ok, thread_id} <-
              do_start_session(port, expanded_workspace, session_policies, dynamic_tool_binding) do
         {:ok,
@@ -304,6 +306,28 @@ defmodule SymphonyElixir.Codex.AppServer do
     Config.codex_runtime_settings(workspace, remote: true)
   end

+  defp apply_runtime_policy(policies, nil), do: {:ok, policies}
+
+  defp apply_runtime_policy(
+         policies,
+         %{
+           "thread_sandbox" => thread_sandbox,
+           "turn_sandbox_policy" => turn_sandbox_policy
+         }
+       )
+       when thread_sandbox in ["read-only", "workspace-write"] and
+              is_map(turn_sandbox_policy) do
+    {:ok,
+     %{
+       policies
+       | thread_sandbox: thread_sandbox,
+         turn_sandbox_policy: turn_sandbox_policy
+     }}
+  end
+
+  defp apply_runtime_policy(_policies, _runtime_policy),
+    do: {:error, :invalid_agentic_cicd_runtime_policy}
+
   defp do_start_session(port, workspace, session_policies, dynamic_tool_binding) do
     case send_initialize(port) do
       :ok -> start_thread(port, workspace, session_policies, dynamic_tool_binding)
diff --git a/elixir/lib/symphony_elixir/config/schema.ex b/elixir/lib/symphony_elixir/config/schema.ex
index 6ebb278..9a9a064 100644
--- a/elixir/lib/symphony_elixir/config/schema.ex
+++ b/elixir/lib/symphony_elixir/config/schema.ex
@@ -215,7 +215,7 @@ defmodule SymphonyElixir.Config.Schema do
       )
       |> validate_required([:command])
       |> validate_change(:command, fn :command, command ->
-        if command != "" and String.trim(command) == "" do
+        if String.trim(command) == "" do
           [command: "can't be blank"]
         else
           []
diff --git a/elixir/lib/symphony_elixir/github/agent_tool.ex b/elixir/lib/symphony_elixir/github/agent_tool.ex
index 87939d8..54f498a 100644
--- a/elixir/lib/symphony_elixir/github/agent_tool.ex
+++ b/elixir/lib/symphony_elixir/github/agent_tool.ex
@@ -8,9 +8,9 @@ defmodule SymphonyElixir.GitHub.AgentTool do
   @github_api_tool "github_api"
   @review_proposal_tool "submit_review_proposal"
   @controller "/opt/jstore-agentic-controller/controller.py"
-  @allowed_methods ["GET", "POST", "PATCH", "PUT", "DELETE"]
+  @allowed_methods ["GET"]
   @github_api_description """
-  Execute a GitHub REST API request using Symphony's configured auth.
+  Execute a read-only GitHub REST API request using Symphony's configured auth.
   """
   @github_api_input_schema %{
     "type" => "object",
@@ -224,7 +224,7 @@ defmodule SymphonyElixir.GitHub.AgentTool do
   end

   defp tool_error_payload(:invalid_method) do
-    %{"error" => %{"message" => "`github_api.method` must be GET, POST, PATCH, PUT, or DELETE."}}
+    %{"error" => %{"message" => "`github_api.method` must be GET in the read-only runtime."}}
   end

   defp tool_error_payload(:invalid_path) do
diff --git a/elixir/lib/symphony_elixir/orchestrator.ex b/elixir/lib/symphony_elixir/orchestrator.ex
index b52e991..986a54a 100644
--- a/elixir/lib/symphony_elixir/orchestrator.ex
+++ b/elixir/lib/symphony_elixir/orchestrator.ex
@@ -1038,7 +1038,8 @@ defmodule SymphonyElixir.Orchestrator do
     delay_ms = retry_delay(next_attempt, metadata)
     old_timer = Map.get(previous_retry, :timer_ref)
     retry_token = make_ref()
-    due_at_ms = System.monotonic_time(:millisecond) + delay_ms
+    scheduled_at_ms = System.monotonic_time(:millisecond)
+    due_at_ms = scheduled_at_ms + delay_ms
     identifier = pick_retry_identifier(issue_id, previous_retry, metadata)
     issue_url = pick_retry_issue_url(previous_retry, metadata)
     error = pick_retry_error(previous_retry, metadata)
@@ -1062,6 +1063,8 @@ defmodule SymphonyElixir.Orchestrator do
             attempt: next_attempt,
             timer_ref: timer_ref,
             retry_token: retry_token,
+            scheduled_at_ms: scheduled_at_ms,
+            delay_ms: delay_ms,
             due_at_ms: due_at_ms,
             identifier: identifier,
             issue_url: issue_url,
diff --git a/elixir/lib/symphony_elixir/prompt_builder.ex b/elixir/lib/symphony_elixir/prompt_builder.ex
index 70c519f..f99c21f 100644
--- a/elixir/lib/symphony_elixir/prompt_builder.ex
+++ b/elixir/lib/symphony_elixir/prompt_builder.ex
@@ -18,6 +18,7 @@ defmodule SymphonyElixir.PromptBuilder do
     |> Solid.render!(
       %{
         "attempt" => Keyword.get(opts, :attempt),
+        "agentic_cicd" => Keyword.get(opts, :agentic_cicd_context, %{}),
         "issue" => issue |> Map.from_struct() |> to_solid_map()
       },
       @render_opts
diff --git a/elixir/lib/symphony_elixir/workspace.ex b/elixir/lib/symphony_elixir/workspace.ex
index 406645b..7d3ef8f 100644
--- a/elixir/lib/symphony_elixir/workspace.ex
+++ b/elixir/lib/symphony_elixir/workspace.ex
@@ -243,7 +243,14 @@ defmodule SymphonyElixir.Workspace do
         :ok

       command ->
-        run_hook(command, workspace, issue_context, "after_run", worker_host, receipt_environment(turn_receipt))
+        run_hook(
+          command,
+          workspace,
+          issue_context,
+          "after_run",
+          worker_host,
+          hook_environment(issue_context, turn_receipt)
+        )
     end
   end

@@ -453,6 +460,28 @@ defmodule SymphonyElixir.Workspace do
     {:error, {:workspace_hook_failed, hook_name, status, output}}
   end

+  defp hook_environment(issue_context, receipt) do
+    phase_context =
+      if is_map(receipt), do: Map.get(receipt, :agentic_cicd_context, %{}), else: %{}
+
+    [
+      {"JSTORE_ISSUE_IDENTIFIER", issue_context.issue_identifier},
+      {"JSTORE_INVOCATION_PHASE", invocation_value(phase_context, "phase")},
+      {"JSTORE_INVOCATION_ROLE", invocation_value(phase_context, "role")},
+      {"JSTORE_INVOCATION_HEAD_SHA", invocation_value(phase_context, "head_sha")},
+      {"JSTORE_INVOCATION_CANDIDATE_REVISION",
+       invocation_value(phase_context, "candidate_revision")}
+      | receipt_environment(receipt)
+    ]
+  end
+
+  defp invocation_value(context, key) do
+    case context[key] do
+      value when is_binary(value) -> value
+      _ -> ""
+    end
+  end
+
   defp receipt_environment(nil), do: []

   defp receipt_environment(receipt) when is_map(receipt) do
diff --git a/elixir/test/symphony_elixir/core_test.exs b/elixir/test/symphony_elixir/core_test.exs
index 1263601..fe4d73c 100644
--- a/elixir/test/symphony_elixir/core_test.exs
+++ b/elixir/test/symphony_elixir/core_test.exs
@@ -1056,7 +1056,7 @@ defmodule SymphonyElixir.CoreTest do
     assert MapSet.member?(state.completed, issue_id)
     assert %{attempt: 1, due_at_ms: due_at_ms} = state.retry_attempts[issue_id]
     assert is_integer(due_at_ms)
-    assert_due_in_range(due_at_ms, 500, 1_100)
+    assert_retry_schedule(state.retry_attempts[issue_id], 1_000)
   end

   test "abnormal worker exit increments retry attempt progressively" do
@@ -1093,10 +1093,10 @@ defmodule SymphonyElixir.CoreTest do
     Process.sleep(50)
     state = :sys.get_state(pid)

-    assert %{attempt: 3, due_at_ms: due_at_ms, identifier: "MT-559", error: "agent exited: :boom"} =
+    assert %{attempt: 3, due_at_ms: _, identifier: "MT-559", error: "agent exited: :boom"} =
              state.retry_attempts[issue_id]

-    assert_due_in_range(due_at_ms, 39_500, 40_500)
+    assert_retry_schedule(state.retry_attempts[issue_id], 40_000)
   end

   test "first abnormal worker exit waits before retrying" do
@@ -1132,10 +1132,10 @@ defmodule SymphonyElixir.CoreTest do
     Process.sleep(50)
     state = :sys.get_state(pid)

-    assert %{attempt: 1, due_at_ms: due_at_ms, identifier: "MT-560", error: "agent exited: :boom"} =
+    assert %{attempt: 1, due_at_ms: _, identifier: "MT-560", error: "agent exited: :boom"} =
              state.retry_attempts[issue_id]

-    assert_due_in_range(due_at_ms, 9_000, 10_500)
+    assert_retry_schedule(state.retry_attempts[issue_id], 10_000)
   end

   test "stale retry timer messages do not consume newer retry entries" do
@@ -1255,11 +1255,12 @@ defmodule SymphonyElixir.CoreTest do
     assert Orchestrator.select_worker_host_for_test(state, "worker-a") == "worker-a"
   end

-  defp assert_due_in_range(due_at_ms, min_remaining_ms, max_remaining_ms) do
-    remaining_ms = due_at_ms - System.monotonic_time(:millisecond)
-
-    assert remaining_ms >= min_remaining_ms
-    assert remaining_ms <= max_remaining_ms
+  defp assert_retry_schedule(
+         %{scheduled_at_ms: scheduled_at_ms, due_at_ms: due_at_ms, delay_ms: delay_ms},
+         expected_delay_ms
+       ) do
+    assert delay_ms == expected_delay_ms
+    assert due_at_ms - scheduled_at_ms == expected_delay_ms
   end

   defp restore_app_env(key, nil), do: Application.delete_env(:symphony_elixir, key)
diff --git a/elixir/test/symphony_elixir/github_adapter_test.exs b/elixir/test/symphony_elixir/github_adapter_test.exs
index 2475853..d841bee 100644
--- a/elixir/test/symphony_elixir/github_adapter_test.exs
+++ b/elixir/test/symphony_elixir/github_adapter_test.exs
@@ -64,7 +64,8 @@ defmodule SymphonyElixir.GitHub.AdapterTest do
     assert {:ok, ["42"]} = GitHubAdapter.fetch_issues_by_ids(["42"])
     assert_receive {:github_ids_called, ["42"]}

-    assert [%{"name" => "github_api"}] = GitHubAdapter.agent_tool_specs()
+    assert ["github_api", "submit_review_proposal"] ==
+             Enum.map(GitHubAdapter.agent_tool_specs(), & &1["name"])

     assert GitHubAdapter.execute_agent_tool(
              "github_api",
@@ -246,22 +247,21 @@ defmodule SymphonyElixir.GitHub.AdapterTest do
       GitHubAgentTool.execute(
         "github_api",
         %{
-          "method" => "post",
-          "path" => " /repos/octo/repo/issues/42/comments ",
-          "params" => %{"per_page" => 10},
-          "body" => %{"body" => "hello"}
+          "method" => "get",
+          "path" => " /repos/octo/repo/issues/42 ",
+          "params" => %{"per_page" => 10}
         },
         tracker_settings: tracker_settings,
         github_client: fn method, path, params, body, opts ->
           send(test_pid, {:github_tool_called, method, path, params, body, opts})
-          {:ok, %{status: 201, body: %{"id" => 9}}}
+          {:ok, %{status: 200, body: %{"id" => 9}}}
         end
       )

-    assert_received {:github_tool_called, "POST", "/repos/octo/repo/issues/42/comments", %{"per_page" => 10}, %{"body" => "hello"}, [tracker_settings: ^tracker_settings]}
+    assert_received {:github_tool_called, "GET", "/repos/octo/repo/issues/42", %{"per_page" => 10}, nil, [tracker_settings: ^tracker_settings]}

     assert response["success"] == true
-    assert Jason.decode!(response["output"]) == %{"status" => 201, "body" => %{"id" => 9}}
+    assert Jason.decode!(response["output"]) == %{"status" => 200, "body" => %{"id" => 9}}
     assert response["contentItems"] == [%{"type" => "inputText", "text" => response["output"]}]

     failure =
@@ -299,12 +299,33 @@ defmodule SymphonyElixir.GitHub.AdapterTest do
         assert invalid["success"] == false
       end
     )
+
+    Enum.each(["POST", "PATCH", "PUT", "DELETE"], fn method ->
+      rejected =
+        GitHubAgentTool.execute(
+          "github_api",
+          %{"method" => method, "path" => "/repos/octo/repo/issues/42"},
+          github_client: fn _method, _path, _params, _body, _opts ->
+            flunk("read-only github_api must reject write methods before calling the client")
+          end
+        )
+
+      assert rejected["success"] == false
+
+      assert Jason.decode!(rejected["output"]) == %{
+               "error" => %{
+                 "message" => "`github_api.method` must be GET in the read-only runtime."
+               }
+             }
+    end)
   end

   test "github_api reports unsupported tools, malformed calls, and client failures" do
     unsupported = GitHubAgentTool.execute("not_github_api", %{}, [])
     assert unsupported["success"] == false
-    assert Jason.decode!(unsupported["output"])["error"]["supportedTools"] == ["github_api"]
+
+    assert Jason.decode!(unsupported["output"])["error"]["supportedTools"] ==
+             ["github_api", "submit_review_proposal"]

     Enum.each(
       [
@@ -392,7 +413,9 @@ defmodule SymphonyElixir.GitHub.AdapterTest do
              token_env
            ]

-    assert [%{"name" => "github_api"}] = binding.tool_specs
+    assert ["github_api", "submit_review_proposal"] ==
+             Enum.map(binding.tool_specs, & &1["name"])
+
     assert :ok = Config.validate!()
   end
