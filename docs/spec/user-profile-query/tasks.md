# 用户资料跨上下文查询实施任务

- [x] 1. 建立 `j-store-user-api` 契约及 User 应用层本地查询实现，测试用户存在、不存在和状态映射。
- [x] 2. 建立受内部 Bearer 凭证保护的 User HTTP 查询端点，测试默认关闭、认证失败、成功和 404。
- [x] 3. 建立 `j-store-user-client-spring` 远程适配器和配置门禁，测试成功、404、依赖失败与 fail-fast。
- [x] 4. 为 Order 增加用户 ACL，破坏性删除客户端可提供的买家资料，并在创建时冻结 ACTIVE 用户快照。
- [x] 5. 将 Shop 的账号存在性适配从 User Repository 切换为用户资料契约。
- [x] 6. 修复订单详情/取消越权与响应 PII 暴露，补充所有权回归测试。
- [x] 7. 分离提供方 reader 与 local/remote 消费 Bean，验证远程模式不会静默回退本地实现。
- [x] 8. 加固远程响应校验和开发验证码日志，覆盖 5xx、空响应、非法资料、用户 ID 不匹配与日志脱敏。
- [x] 9. 更新模块索引、部署配置和领域说明，运行完整质量门禁、打包及独立安全复评。
